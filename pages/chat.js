import React from "react";
import styled from "styled-components";

const Chat = () => {
	return <ChatContainer>chat container</ChatContainer>;
};

export default Chat;

const ChatContainer = styled.div``;
